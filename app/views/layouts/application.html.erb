<!DOCTYPE html>
<html>
<head>
  <title>Lets meet</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="container">
    <div id="nav"> 
      <h1 id="title"> Maven </h1>
      <ul>
      <li><%= link_to "Projects", projects_url %></li>
      <li><%= link_to "Your Profile", user_projects_path(current_user) %></li>
        <% if current_user %>
            <li>Signed in as <%= current_user.email %> <%= link_to "Log out", session_path("current"), :method => :delete %></li>
        <% else %>
            <li><%= link_to "Log in", new_session_path %> or <%= link_to "Sign up", new_user_path %></li>
        <% end %>
      </ul>
    </div>

  <div style="clear:both;"></div>
    <% if flash[:notice] %>
    	 <p><%= flash[:notice] %></p>
    <% end %>
    
    <% if flash[:alert] %>
    	 <p><%= flash[:alert] %></p>
    <% end %>

<%= yield %>
  <%= debug params %>
 </div>
</body>
</html>
